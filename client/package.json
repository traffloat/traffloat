{
  "author": "SOFe <sofe2038@gmail.com>",
  "name": "traffloat",
  "version": "0.1.0",
  "repository": "https://github.com/SOF3/traffloat",
  "license": "AGPL-3.0",
  "scripts": {
    "build": "rimraf dist pkg && python3 textures/combine.py && webpack",
    "build-dev": "rimraf dist pkg && python3 textures/combine.py && webpack --mode development",
    "texture": "python3 textures/combine.py",
    "start": "rimraf dist pkg && webpack serve --mode development --open",
    "test": "cargo test && wasm-pack test --headless"
  },
  "devDependencies": {
    "@wasm-tool/wasm-pack-plugin": "^1.4.0",
    "copy-webpack-plugin": "^9.0.1",
    "rimraf": "^3.0.0",
    "webpack": "^5.40.0",
    "webpack-cli": "^4.6.0",
    "webpack-dev-server": "^3.11.2"
  }
}
