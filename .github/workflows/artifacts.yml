name: CI
on:
  push:
    branches:
      - "**"
      - "!dependabot/**"
    tags: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  build:
    name: earthly build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: earthly/actions-setup@v1
        with:
          version: v0.8.0

      - run: earthly +compile
      - uses: actions/upload-artifact@v4
        with:
          name: traffloat-desktop-linux
          path: output/traffloat-desktop

      - run: earthly +save-schema
      - uses: actions/upload-artifact@v4
        with:
          name: save-schema.json
          path: output/save-schema.json

      - run: earthly +assets
      - uses: actions/upload-artifact@v4
        with:
          name: assets
          path: assets/
